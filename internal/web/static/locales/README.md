# Locales

Traeky loads translations from JSON files in this directory. English (`en.json`) is the fallback language. Add a new language by creating another JSON file with the same keys and then adding the language code to `SUPPORTED_LOCALES` in `app.js`.
